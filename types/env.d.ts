declare let process: {
  env: {
    ENV_MODE: string; // customized environment mode
    NODE_ENV: string; // by default used by react native
    API_URL: string;
    EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY: string;
  };
};
